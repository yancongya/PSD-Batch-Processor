name: build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Debug environment
        run: |
          Write-Host "Current directory: $(Get-Location)"
          Write-Host "Files in current directory:"
          Get-ChildItem | Select-Object Name
          Write-Host "Files in .github directory:"
          Get-ChildItem .github | Select-Object Name
      - run: python .github/scripts/build.py
      - uses: softprops/action-gh-release@v1
        if: github.event_name == 'push'
        with:
          files: |
            dist/PSDBatchProcessor-Windowed/*
            dist/PSDBatchProcessor-Console/*
            dist/PSDBatchProcessor-OneFile.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}